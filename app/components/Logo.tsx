/** JazzWorld brand mark — official logo, cropped to the circular monogram. */
export function Logo() {
  return (
    <span className="mark" aria-hidden="true">
      {/* eslint-disable-next-line @next/next/no-img-element */}
      <img src="/jazzworld-logo.png" alt="" />
    </span>
  );
}
